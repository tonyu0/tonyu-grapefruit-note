services:
  app:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app # binding source code
      - /app/node_modules # creating volume for node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true # for hot reload problems in Windows environments, etc.
